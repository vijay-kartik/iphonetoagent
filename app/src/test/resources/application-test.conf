ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ org.example.ApplicationKt.module ]
    }
}

server {
    port = 8080
    host = "0.0.0.0"
}

notion {
    apiToken = "ntn_fake_test_token_for_testing_purposes_only"
    apiVersion = "2022-06-28"
    databaseId = "test-db-id"
    baseUrl = "https://api.notion.com"
}

security {
    apiKey = "test-api-key"
}
